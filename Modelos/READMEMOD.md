# 📂 Carpeta: Modelos

Aquí se guarda el modelo entrenado (`modelo_cnn.pth`) generado tras correr `main.py`.

✔️ Este modelo será usado por `generar_reporte.py` para clasificar nuevas imágenes.

Puedes guardar múltiples versiones con nombres diferentes si lo deseas.
